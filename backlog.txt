- setup menuToggle and menuNumber
- hook in all the menus
- save settings (and split place) as cookie (or whatever)
- save script as cookie (or whatever)

- look into opening and saving in client js

- fix renderer
	- setColor(value) value = If array: color is oneColor, if buffer: color is vertColor
	- setAmbient(value) value = float representing ambience
	- setLighting(VIEW || TOP || TOP_BOTTOM)

- display labels
- hovering over points in the 3d scene highlights them (?) and brings up their info in statusbar

